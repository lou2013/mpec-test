export enum RelationTypesEnum {
  grounds = 'grounds',
  enables = 'enables',
  requires = 'requires',
  produces = 'produces',
  decomposes_to = 'decomposes_to',
  applies = 'applies',
  evaluates_to = 'evaluates_to',
}
